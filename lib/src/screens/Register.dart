import 'package:flutter/material.dart';

class Registerpagina extends StatelessWidget {
  const Registerpagina({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}